--- /usr/local/lib/python3.12/dist-packages/mlflow/store/tracking/file_store.py	2026-01-30 21:33:29.000000000 +0100
+++ /usr/local/lib/python3.12/dist-packages/mlflow/store/tracking/file_store_fixed.py	2026-02-02 19:21:03.330264050 +0100
@@ -710,7 +710,8 @@
         new_info = run_info._copy_with_overrides(run_status, end_time, run_name=run_name)
         if run_name:
             self._set_run_tag(run_info, RunTag(MLFLOW_RUN_NAME, run_name))
-        self._overwrite_run_info(new_info)
+        if new_info != run_info:
+            self._overwrite_run_info(new_info)
         return new_info
 
     def create_run(self, experiment_id, user_id, start_time, tags, run_name):
@@ -1231,7 +1232,7 @@
         run_info_dict = _make_persisted_run_info_dict(run_info)
         if deleted_time is not None:
             run_info_dict["deleted_time"] = deleted_time
-        write_yaml(run_dir, FileStore.META_DATA_FILE_NAME, run_info_dict, overwrite=True)
+        overwrite_yaml(run_dir, FileStore.META_DATA_FILE_NAME, run_info_dict)
 
     def log_batch(self, run_id, metrics, params, tags):
         _validate_run_id(run_id)
